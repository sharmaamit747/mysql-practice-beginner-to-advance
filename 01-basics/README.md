# SQL Basics

This folder covers basic SQL commands required in every interview.

## Topics
- SELECT
- DISTINCT
- WHERE
- ORDER BY
- LIMIT

## Execution Order
FROM → WHERE → SELECT → ORDER BY → LIMIT

Practice these queries until they feel natural.

